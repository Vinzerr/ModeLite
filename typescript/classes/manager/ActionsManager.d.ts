import { Client, BaseManager } from '../../index';
export declare class ActionsManager extends BaseManager {
    [key: string]: any;
    constructor(client: Client);
    register(context_piece: any): void;
}
